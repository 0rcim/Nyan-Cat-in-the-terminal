# Description
基于ANSI转义序列

# Install Dependency

* `npm i @types/node`

# Build

* `tsc -p ./tsconfig.json`

# Run

```bash
node ./build/app.js Black.cat # load Black.cat
node ./build/app.js # default: Gray.cat
#!# 确保窗口有足够大小
```